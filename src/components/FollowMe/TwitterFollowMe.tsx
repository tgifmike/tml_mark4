import React from 'react';
import { XIcon } from 'react-share';

const TwitterFollowMe = () => {
	return (
		<main className="mr-2">
			<a href="https://twitter.com/themanagerlife" data-size="large">
				<XIcon size={28} round={true} />
			</a>
		</main>
	);
};

export default TwitterFollowMe;
